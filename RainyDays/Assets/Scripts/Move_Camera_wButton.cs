using UnityEngine;

public class Move_Camera_wButton : MonoBehaviour
{
    //public leftButton;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void leftButton()
    {
        
    }
}
